# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage
coverage.xml

# --- Node / frontend ---
node_modules/
.vite/
frontend/dist/
*.tsbuildinfo
npm-debug.log*
pnpm-debug.log*
yarn-error.log*

# --- Built frontend, written into the package by `vite build` ---
# Not tracked at all: the build wipes this directory (emptyOutDir), which
# would leave a tracked placeholder permanently "deleted" in every deployment.
server/app/static/

# Written by install.sh so /api/version can report the commit without git.
server/app/_build_info.py

# --- PlatformIO / ESP-IDF ---
.pio/
.pioenvs/
.piolibdeps/
firmware/sdkconfig
firmware/sdkconfig.*
!firmware/sdkconfig.defaults
firmware/build/
firmware/managed_components/
firmware/dependencies.lock

# --- Secrets & local state (NEVER commit) ---
*.env
.env
.env.*
!.env.example
turret.env
firmware/include/secrets.h
server/data/
*.db
*.db-journal
*.db-wal
*.db-shm
models/*.pt
models/*.onnx
models/*.engine
snapshots/

# --- Editors / OS ---
.vscode/*
!.vscode/extensions.json
!.vscode/settings.example.json
.claude/settings.local.json
.idea/
.DS_Store
Thumbs.db
*.swp
